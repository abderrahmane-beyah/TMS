# Clé API Google Maps (optionnel - pour routing alternatif)
VITE_GOOGLE_MAPS_API_KEY=votre-cle-google-maps-optionnel

# Note : VITE_API_URL est configuré dans docker-compose.prod.yml comme argument de build
# En développement local, le frontend utilise http://localhost:8000/api/v1 par défaut
